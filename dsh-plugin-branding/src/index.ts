/** Branding Host entry: intentionally inert. This plugin only contributes a
 * client face; the loader row exists to compose the package into a profile. */

export const name = 'branding'

export const inject: readonly string[] = []

export function apply(): void {}
